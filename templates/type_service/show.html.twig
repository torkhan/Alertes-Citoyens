{% extends 'base.html.twig' %}

{% block title %}Types de Services{% endblock %}

{% block body %}
    <h1>Types de Services</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_service.id }}</td>
            </tr>
            <tr>
                <th>ServiceType</th>
                <td>{{ type_service.serviceType }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('type_service_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('type_service_edit', {'id': type_service.id}) }}">Editer</a>

    {{ include('type_service/_delete_form.html.twig') }}
{% endblock %}
