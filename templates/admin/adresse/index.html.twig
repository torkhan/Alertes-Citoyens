{% extends 'base.html.twig' %}

{% block title %}Liste des villes{% endblock %}

{% block body %}
    <h1 class="text-center mb-4">Liste des villes</h1>


    <table class="table-responsive">
        <thead>
            <tr>
                <th>Ville</th>
                <th>Cp</th>
                <th>Departement</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for adresse in adresses %}
            <tr >

                <td>{{ adresse.nomVille }}</td>
                <td>{{ adresse.cp }}</td>
                <td>{{ adresse.nomDepartement }}</td>
                <td>
                    {#<a class="btn btn-info" href="{{ path('adresse_show', {'id': adresse.id}) }}">Voir</a>#}
                    <a class="btn btn-warning" href="{{ path('adresse_edit', {'id': adresse.id}) }}">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="p-2 my-4">
        <a class="btn btn-success my-4 mr-5" href="{{ path('adresse_new') }}">Ajouter une ville</a>
        <a class="btn btn-primary ml-5" href="{{ path('plateforme_admin') }}">Retour plateforme</a>
    </div>


{% endblock %}
