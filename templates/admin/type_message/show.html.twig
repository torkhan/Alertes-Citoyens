{% extends 'base.html.twig' %}

{% block title %}Types de messages{% endblock %}

{% block body %}
    <h1>Types de messages</h1>

    <table class="table-responsive">
        <tbody>

            <tr>
                <th>MessageType</th>
                <td>{{ type_message.messageType }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('type_message_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('type_message_edit', {'id': type_message.id}) }}">Editer</a>

    {{ include('admin/type_message/_delete_form.html.twig') }}
{% endblock %}
