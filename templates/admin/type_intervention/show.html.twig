{% extends 'base.html.twig' %}

{% block title %}Types d'nterventions{% endblock %}

{% block body %}
    <h1>Types d'nterventions</h1>

    <table class="table-responsive">
        <tbody>

            <tr>
                <th>Type d'ntervention</th>
                <td>{{ type_intervention.interventionType }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('type_intervention_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('type_intervention_edit', {'id': type_intervention.id}) }}">Editer</a>

    {{ include('admin/type_intervention/_delete_form.html.twig') }}
{% endblock %}
