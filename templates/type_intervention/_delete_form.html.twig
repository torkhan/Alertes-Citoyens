<form method="post" action="{{ path('type_intervention_delete', {'id': type_intervention.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_intervention.id) }}">
    <button class="btn btn-danger">Effacer</button>
</form>
