{% extends 'base.html.twig' %}

{% block title %}Edit Destinataire{% endblock %}

{% block body %}
    <h1> Destinataire</h1>

    {{ include('destinataire/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary" href="{{ path('destinataire_index') }}">Retour à la liste</a>

    {{ include('destinataire/_delete_form.html.twig') }}
{% endblock %}
{% block stylesheets %}
    <script src="{{ asset("public/js/inscription.js") }}"></script>
{% endblock %}
