{% extends 'base.html.twig' %}

{% block title %}Edition des Messages{% endblock %}

{% block body %}
    <h1 class="text-center mb-4">Modifications</h1>
    <div class="container w-75">

    {{ include('message/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary my-3" href="{{ path('message_index') }}">Retour à la liste</a>

    {{ include('message/_delete_form.html.twig') }}
    </div>
{% endblock %}
