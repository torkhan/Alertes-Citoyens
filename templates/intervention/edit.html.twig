{% extends 'base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block body %}
    <h1 class="text-center mb-4">Modification</h1>
    <div class="container w-75">

    {{ include('intervention/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary my-3" href="{{ path('intervention_index') }}">retour à la liste</a>

    {{ include('intervention/_delete_form.html.twig') }}
    </div>
{% endblock %}
