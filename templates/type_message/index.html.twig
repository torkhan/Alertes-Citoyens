{% extends 'base.html.twig' %}

{% block title %}Type de messages{% endblock %}

{% block body %}
    <h1 class="text-center mb-4">Titres des alertes</h1>
<div class="container w-75">
    <table class="table-responsive">
        <thead>
            <tr>

                <th>Titres</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_message in type_messages %}
            <tr>
               
                <td>{{ type_message.messageType }}</td>
                <td>
                    {#<a class="btn btn-info" href="{{ path('type_message_show', {'id': type_message.id}) }}">Voir</a>#}
                    <a class="btn btn-warning" href="{{ path('type_message_edit', {'id': type_message.id}) }}">editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('type_message_new') }}">Créer nouveau type de message</a>

</div>
{% endblock %}
